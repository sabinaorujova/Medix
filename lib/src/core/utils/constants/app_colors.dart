import 'package:flutter/material.dart';

/// Application color constants.
/// 
/// Contains all color definitions used throughout the app.
/// All colors are defined as static const values.
class AppColors {
  AppColors._();
  static const primaryFirst = Color(0xff1FA0BD);
  static const primarySecend = Color(0xff5FD8F3);
  static const white = Color(0xffffffff);
  static const backWhite = Color.fromARGB(255, 244, 244, 247);
  static const black = Color(0xff000000);
  static const grey = Color(0xff2D2D2D);
  static const Color neutralLight = Color(0xFFEBF0FF);
  static const Color red = Colors.red;
  static const Color orange = Color.fromRGBO(246, 169, 54, 1);
  static const Color transparent = Colors.transparent;
  static const Color neutralGrey = Color(0xFF9098B1);
  static const Color yellow = Color.fromRGBO(17, 84, 100, 0.8);
  static const Color neutralBlue = Color(0xFFEEF8FA);
  static const Color bookappointmentback = Color.fromRGBO(242, 252, 255, 1);
    static const Color cameraContanierColor = Color.fromRGBO(242, 252, 255, 1);
      static const Color greyopasty = Color.fromRGBO(185, 185, 185, 0.8);

}